# 归档目录

此目录存放项目历史文件和旧版本代码。

## 目录结构

- `old_code/`: 旧版本的代码文件
  - `all.py`: 全参数扫描脚本（旧版）
  - `battery.py`: 监测脚本（旧版）
  - `参考.py`: 参考代码

- `old_output/`: 旧版本的输出文件
  - `all.txt`: 扫描结果（旧版）
  - `battrey.txt`: 监测日志（旧版）

## 说明

这些文件已不再使用，保留仅用于参考。当前项目已重构为模块化结构，请使用 `src/` 目录下的新代码。

